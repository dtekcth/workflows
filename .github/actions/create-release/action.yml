name: "Create release"
description: "Creates, tags and pushes a release commit as webredax. Includes uncommitted changes if there are any."
inputs:
  new_version:
    required: true
    description: "v*.*.* or patch/minor/major"
outputs:
  new_version:
    description: "Same as input or calculated based on update type"
runs:
  using: "docker"
  image: "Dockerfile"
