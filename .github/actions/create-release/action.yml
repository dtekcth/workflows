# action.yml
name: "Create release"
description: "Greet someone and record the time"
inputs:
  new_version:
    required: true
runs:
  using: "docker"
  image: "docker://alpine/git:v2.55"
  entrypoint: entrypoint.sh
